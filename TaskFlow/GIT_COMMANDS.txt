Для commit и push в main branch выполните следующие команды:

1. git add .
2. git commit -m "Fix task deletion: remove confirmation modal and add compact undo notification"
3. git push origin main

Или запустите batch файл: commit-and-push.bat

Или PowerShell скрипт: commit-and-push.ps1
